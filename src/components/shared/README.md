# Shared Components

Reusable UI primitives live here when they are used across multiple feature areas.

Current components are still small enough to stay near their feature folders. This folder exists to match the V1 structure and keep future shared controls out of feature-specific modules.
